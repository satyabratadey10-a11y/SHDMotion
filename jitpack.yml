jdk:
  - openjdk17

before_install:
  - sdkmanager "platforms;android-34" "build-tools;34.0.0" "ndk;25.2.9519653"

install:
  - ./gradlew :motion-engine:assembleRelease --no-daemon

artifacts:
  - motion-engine/build/outputs/aar/motion-engine-release.aar
